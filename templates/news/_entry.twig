{% extends "_layouts/main" %}

{% block main %}

<h1>{{ entry.title }}</h1>
<h2>{{ entry.newsSubtitle }}</h2>
<p>{{ entry.newsContent }}</p>
<img src="{{ entry.newsImage.one().url() }}" alt="">

{%  endblock %}